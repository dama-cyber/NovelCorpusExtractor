# 小说语料提取系统依赖

# 核心依赖
PyYAML>=6.0
tqdm>=4.66.0

# 模型API客户端（根据需要选择安装）
openai>=1.0.0  # OpenAI API
anthropic>=0.18.0  # Anthropic Claude API
google-generativeai>=0.3.0  # Google Gemini API
aiohttp>=3.9.0  # 异步HTTP客户端（多API支持必需）

# 向量数据库（可选）
chromadb>=0.4.0  # 用于Frankentexts向量检索

# 其他工具
python-dotenv>=1.0.0  # 环境变量管理

